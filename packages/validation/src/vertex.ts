// import { Vertex } from "@ts-drp/types";
// import { computeHash } from "@ts-drp/utils";

// export function validateVertexHash(vertex: Vertex): void {
// 	const expected = computeHash(
// 		vertex.peerId,
// 		vertex.operation,
// 		vertex.dependencies,
// 		vertex.timestamp
// 	);
// 	if (vertex.hash !== expected) {
// 		throw new Error(`Invalid hash for vertex ${vertex.hash}`);
// 	}
// }
